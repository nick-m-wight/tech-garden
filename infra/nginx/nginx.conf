# Placeholder. Real reverse-proxy + TLS termination config arrives in
# CLAUDE.md Section 16 step 12 (docker-compose work).
# Will reference nginx.dev.conf and nginx.prod.conf as docker-compose mounts them.
